package application.base.domain.sharedBoardManagement;

public enum UserPermission {
        /**
         * Indicates that the User can only read the information from the shared board
         */
        READ_ONLY,

        /**
         * Indicates that the User can read and write information on the shared board
         */
        WRITE
}
